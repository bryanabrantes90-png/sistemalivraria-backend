# Porta do servidor (igual configurado)
server.port=${SERVER_PORT:10000}
server.address=${SERVER_ADDRESS:0.0.0.0}

# Conexão com o banco Railway (usando exatamente os dados da imagem)
spring.datasource.url=jdbc:mysql://thomas.proxy.rlwy.net:47919/cake_orderling?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=IWpcdhQMzSvUWnIUQImXKSfOcVUYjEmB
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configurações JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.open-in-view=false

# Desabilita tela de login do Spring Security
spring.security.enabled=false
spring.application.name=backend

# ✅ URL INTERNA DO RAILWAY (funciona só dentro do Railway, é a correta)
spring.datasource.url=jdbc:mysql://${mysql.railway.internal}:${3306}/${railway}?useSSL=false&serverTimezone=America/Sao_Paulo&allowPublicKeyRetrieval=true

# ✅ Variáveis padrão do Railway (ele cria essas automaticamente)
spring.datasource.username=${root}
spring.datasource.password=${IWpcdhQMzSvUWnIUQImXKSfOcVUYjEmB}

# ✅ Driver correto
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ✅ JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ❌ REMOVIDA (não usa mais)
# spring.jpa.properties.hibernate.dialect=...

# ✅ Porta do Railway (obrigatório)
server.port=${PORT:8085}

# ✅ Conexão estável
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=20000
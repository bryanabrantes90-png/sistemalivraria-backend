# ✅ PORTA OBRIGATÓRIA (Render usa 10000 por padrão)
server.port=${PORT:10000}

# ✅ CONEXÃO COM MYSQL — FORMATO COMPATÍVEL COM RENDER E RAILWAY
# Se as variáveis não existirem, usa valores padrão (para teste local)
spring.datasource.url=jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:railway}?useSSL=true&requireSSL=false&serverTimezone=America/Sao_Paulo&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:IWpcdhQMzSvUWnIUQImXKSfOcVUYjEmB}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ✅ CONFIGS JPA (removi a linha que estava dando aviso)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# ✅ UPLOADS (pasta temporária na nuvem)
livraria.upload.dir=/tmp/uploads/